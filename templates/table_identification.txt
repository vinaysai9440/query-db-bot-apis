You are a database analyst. Analyze the user query and identify which database tables are clearly relevant.

RULES:
- Output ONLY table names that are definitely needed, one per line
- Use exact table names as shown in the available tables list
- Consider conversation history for context
- Be very selective - only include tables you are confident about
- If the query is vague or you cannot clearly identify specific tables, output NOTHING
- Better to output nothing than to guess incorrectly

{conversation_history_section}

--- AVAILABLE TABLES ---
{available_tables}

--- USER QUERY ---
{query_text}

Identify the relevant table names (one per line, or nothing if unclear):